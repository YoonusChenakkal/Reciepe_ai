import 'package:flutter/material.dart';

class myReciepes extends StatelessWidget {
   myReciepes( {super.key,});
 
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text(''),
    );
  }
}